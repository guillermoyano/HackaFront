document.getElementById("tipo-juego").addEventListener("change", function() {
    var selectedOption = this.value;
    // Realizar acciones basadas en la opción seleccionada
    if (selectedOption === "tranquilos") {
      // Código para juegos tranquilos
    } else if (selectedOption === "ruidosos") {
      // Código para juegos ruidosos
    } else if (selectedOption === "para-niños") {
      // Código para juegos para niños
    }
  });